/**
 * Canonical site origin — the single source of truth for absolute URLs
 * (metadata, canonical links, sitemap, robots, share links). Update this one
 * value if the domain ever changes again.
 */
export const SITE_URL = "https://www.yourdecornest.com";

export const SITE_NAME = "DecorNest";
